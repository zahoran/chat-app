{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat app using websockets",
  "main": "index.js",
  "scripts": {
    "dev": "env-cmd -f config/dev.env nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Zahoran",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "nodemon": "^2.0.4"
  }
}
